# Legacy Scripts

This folder keeps older preprocessing and experiment scripts from the original private project.

- They are kept as supplemental reference.
- They are not required for the GitHub demo quickstart.
- The public-facing live demo path uses `app/`, `scripts/`, `docs/`, and `tests/`.
