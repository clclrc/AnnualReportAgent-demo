# Raw PDF Boundary

The full raw annual report PDF corpus is not included in this public demo repository.

Paths inside `data/*_info.json` were rewritten to the placeholder form:

```text
data/raw_pdfs/<annual-report-file-name>.pdf
```

That keeps the original data shape useful for code review while removing private filesystem locations and large non-public artifacts.
